<%= simple_form_for([@current_sitter, @booking_date]) do |f| %>
  <%= f.input :sitter_id, as: :hidden, input_html: { value: @current_sitter.id} %>
  <%= f.input :date, label: false, input_html:{ for: 'booking_date_date'} %>

  <%= f.submit '送出', class: 'btn btn-primary'%>
<% end %>