
<!-- Image and text -->
<nav class="navbar navbar-light bg-light navbar-fluid">
    <ul>
        <li class="navbar-left">
            <%= link_to image_tag("animals.png"), root_path, class: 'nav-logo' %>
            <%= link_to 'PetCollage', root_path, class: 'nav-st' %>
          <% if user_signed_in? && current_user.role == 'user' %>
            <%= link_to '成為保母', sitters_path %>
          <% elsif user_signed_in? && current_user.role == 'sitter' %>
            <a href=""></a>
          <% else %>
            <%= link_to '成為保母', user_session_path %>
          <% end %>
        </li>
        <li class="navnbar-right nav-item dropdown navbar-nav">
          <% if user_signed_in? %>
            <% current_user.role %>
            <% @current_sitter = Sitter.find_by("name == '#{current_user.name}'") %>
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= "Hi, #{current_user.name}" %><img src="<%= current_user.avatar %>" alt="" class="avatar-img">
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <% if current_user.role == "sitter" %>
              <%= link_to '個人資料', sitter_path(@current_sitter.id), class: 'dropdown-item' %>
            <% else %>  
              <%= link_to '個人資料', user_path(current_user), class: 'dropdown-item' %>
            <% end %>
              <%= link_to '我的訂單', user_orders_path(current_user), class: 'dropdown-item' %>
              <%= link_to "登出", destroy_user_session_path, method: "delete", class: 'dropdown-item' %>
            </div>
          <% else %>
            <%= link_to '登入', new_user_session_path %>
            <%= link_to '註冊', new_user_registration_path %>
          <% end %>
        </li>
    </ul>
</nav>