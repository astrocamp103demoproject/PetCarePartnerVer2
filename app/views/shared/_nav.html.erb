<nav class='top-nav'>
  <ul>
    <span class="nav-left">
      <li><%= link_to 'PetCollage', root_path %></li>

      <% if user_signed_in?%>
        <li><%= link_to 'Become Sitter', sitters_path %></li>
      <% else %>
        <li><%= link_to 'Become Sitter', user_session_path %></li>
      <%end%>

    </span>

<span class="nav-right">
    <span class="dropdown">
    <% if user_signed_in?   %>
      <div class="dropbtn" >
        <%= "Hi, #{current_user.name}" %>
        <img src="<%= current_user.avatar %>" alt="" class="avatar-img">
      </div>
        </button>
        <div class="dropdown-content">
        <li><%= link_to 'My Profile', user_path(current_user) %></li>
        <li><%= link_to 'My Order', user_orders_path(current_user) %></li>
        <li><%= link_to "Log out", destroy_user_session_path, method: "delete" %></li>
        </div>
    <% else %>
        <li><%= link_to 'Log in', new_user_session_path %></li>
        <li><%= link_to 'Sign up', new_user_registration_path %></li>
    <% end %>
    
    </span>
</span>
  

    
  </ul>
</nav>