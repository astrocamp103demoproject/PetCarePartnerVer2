<%= simple_form_for ([@current_sitter,@picture]) do |f| %> 
    
    <%= f.input :sitter_id, as: :hidden, input_html: { value: @current_sitter} %>
    <%= f.input :pic %>
    <%= f.submit '新增', class: 'btn btn-success'%>
<% end %>
